name: Deploy on deploy branch

on:
  push:
    branches:
      - deploy

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3

      - name: Deploy with docker-compose
        run: echo "hey from the other side"
